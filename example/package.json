{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "doc": "typedoc-declare --out docs --mode file --declarationFile docs/index.d.ts --omitTag since --omitTag other",
    "declaration": "typedoc-declare --mode file --declarationOnly --declarationFile docs/index.d.ts --omitTag since --omitTag other",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typedoc-plugin-typescript-declaration": "file:.."
  },
  "dependencies": {}
}
