#!/usr/bin/env node

const package = require('../package.json');
const path = require('path');

const { CliApplication } = require('../dist/index');

const app = new CliApplication({
  plugin: [path.resolve(__dirname, '..')],
});
app.start();
